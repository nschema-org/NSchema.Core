namespace NSchema.Operations.Apply;

/// <summary>
/// Arguments for an <see cref="IApplyOperation"/> run. Empty today; the home for future apply inputs.
/// </summary>
public sealed record ApplyArguments;
