+ schema reporting ("analytics")
    + grant usage to analyst

+ table app.users ("all users")
    + id bigint not null
    + name varchar(255) not null

    + primary key users_pkey
    + unique constraint users_email_uq
    + check constraint users_age_chk
    + index users_name_ix
    + grant SELECT to readers

~ table app.purchases → orders
    ~ total type: int → bigint
    ~ total nullable: null → not null
    - legacy_flag boolean not null
    ~ primary key orders_pkey comment: "old note" → "new note"
    - foreign key orders_user_fk
    - unique constraint orders_code_uq
    - check constraint orders_total_chk
    - revoke INSERT from writers

- schema scratch

Plan: 8 to add, 3 to change, 5 to destroy.