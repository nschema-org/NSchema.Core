+ view app.active_users ("currently active users")

~ view app.daily_totals

~ view app.summary comment: "old summary" → "new summary"

~ view app.legacy_report → report

- view app.stale_view

+ materialized view app.mv_sales ("sales rollup")

~ materialized view app.mv_active
    + index mv_active_ix
    - index mv_active_old_ix

Plan: 2 to add, 4 to change, 1 to destroy.